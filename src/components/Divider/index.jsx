import React from "react";
import * as S from "./index.styles";

/**
 * Divider Component
 *
 * Renders a visual divider element for separating sections on the page.
 *
 * @component
 * @returns {JSX.Element} The rendered Divider component.
 */

const Divider = () => {
  return <S.Divider />;
};

export default Divider;
